import { Typography } from "@mui/material";

const Title = () => {
  return (
    <Typography
      sx={{
        color: "#EEEEEE",
        textAlign: "center",
        fontSize: "3rem",
        marginTop: "100px",
        fontFamily: "open-sans",
        fontWeight: 'bold'
      }}
    >
      Smart Search
    </Typography>
  );
};

export default Title;
